class Product {
    /**
     * Product constructor.
     * @param {String} name 
     * @param {Number} price 
     * @param {Number} amount 
     */
    constructor(name, price, amount) {
        this.name = name;
        this.price = price;
        this.amount = amount;
    }
}

module.exports = Product